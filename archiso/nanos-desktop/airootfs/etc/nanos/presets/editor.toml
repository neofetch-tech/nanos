[preset]
name = "editor"
description = "VS Code (lightweight, no JetBrains — see nanOS efficiency notes)"
profile = ["desktop"]

[packages]
pacman = ["curl", "tar"]

[hooks]
post_install = "hooks/editor-post.sh"
